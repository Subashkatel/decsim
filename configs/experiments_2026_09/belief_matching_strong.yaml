# Pair 1, the strong tier alone: belief matching (ldpc's BP then pymatching).
# Every other key is base.yaml's.

extends: base.yaml

escalation:
  kind: strong_only

strong_decoder:
  kind: belief_matching
  units: 1
  unit_memory_rounds: null
  engine:
    clock: room
    fetch_cycles_per_round: 1
    fetch_cycles_per_job: 0
    release_cycles_per_job: 10
    release_cycles_per_round: 0
