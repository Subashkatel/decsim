# Pair 1, the weak tier alone: pymatching (sparse blossom, C++).
# Every other key is base.yaml's.

extends: base.yaml

escalation:
  kind: weak_baseline

weak_decoder:
  kind: pymatching
  units: 1
  unit_memory_rounds: null
  engine:
    clock: fridge
    fetch_cycles_per_round: 1
    release_cycles_per_job: 10
