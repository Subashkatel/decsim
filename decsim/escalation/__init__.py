"""The escalation: a window decoded again by the strong tier.

Whether, when and how (Toshio et al. 2510.25222). The policy rows decide
(policies.py, on the EscalationPolicy port) with a threshold from a
ThresholdSource row (threshold_sources.py); the strong window's shape
lays out the re-decode (strong_window_shapes.py); the StrongRedecode
selects, submits and lands it (strong_redecode.py).
"""
