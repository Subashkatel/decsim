"""The escalation: a window decoded again by the strong tier.

Whether, when and how (Toshio et al. 2510.25222). The policy rows decide
(decoders/weak_strong_switching.py, on the EscalationPolicy port); the
strong window's shape lays out the re-decode (strong_window_shapes.py);
the StrongRedecode selects, submits and lands it (strong_redecode.py).
"""
