# Early look at the strong tier's latency run: 100 shots per d while the
# 500-shot strong_latency job finishes. Same axes, fewer samples;
# retired once the full run lands. Every key is documented in
# reference.yaml.

extends: strong_decoder_baseline.yaml

sweep:
  - physical_error_probability: [0.001]
    distance: [3, 5, 7, 9, 11]
    round_period_us: [1.0]
    shots: 100
