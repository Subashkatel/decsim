# The stress recipe from the 2026-08-26 meeting: long uninterrupted memory,
# hundreds of windows per shot, real MWPM per window.
extends: weak_baseline.yaml

distance: 9
rounds_per_shot: 10000

sweep:
  - physical_error_probability: [0.001]
    round_period_us: [1.0]
    algorithm_latency_us: [pymatching]
    shots: 2

trace: off
