# Frozen QLX test fixtures

The 25 fixture and provenance files in this directory were restored
byte-for-byte from Git revision `43f4ffa^` (resolved commit
`811d3e1c0e6a072230ea095c7d87440e4bf44fb7`). They are frozen, test-only
inputs. Never regenerate them in CI.

The generator, dump, and probe scripts are retained only as provenance.
Tests must not import or execute those scripts.
